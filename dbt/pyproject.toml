[project]
name = "gloomberg-dbt"
version = "0.1.0"
description = "Gloomberg Terminal medallion transform — dbt project (docs/architecture/02)"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.9",
    "dbt-duckdb>=1.9",
    "dbt-postgres>=1.9",
]

[tool.uv]
package = false
