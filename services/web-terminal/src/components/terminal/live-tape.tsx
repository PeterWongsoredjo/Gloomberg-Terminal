import { EmptyPanelState } from "@/components/terminal/empty-panel-state";

/** Live Tape — streams agg_live_tape over WebSocket once serving lands (SV-03). */
export function LiveTape() {
  return <EmptyPanelState title="Live Tape" />;
}
