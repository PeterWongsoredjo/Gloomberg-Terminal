import { EmptyPanelState } from "@/components/terminal/empty-panel-state";

/** Sentiment Matrix — sector × label grid over fct_sentiment once serving lands (SV-04). */
export function SentimentMatrix() {
  return <EmptyPanelState title="Sentiment Matrix" />;
}
