import { EmptyPanelState } from "@/components/terminal/empty-panel-state";

/** Data Telemetry — coverage/freshness/degradation rollup once observability lands (SV-06). */
export function DataTelemetry() {
  return <EmptyPanelState title="Data Telemetry" />;
}
