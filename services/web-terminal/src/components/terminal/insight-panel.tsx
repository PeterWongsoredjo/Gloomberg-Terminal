import { EmptyPanelState } from "@/components/terminal/empty-panel-state";

/** Insight Panel — non-advisory CT-009-derived narrative once serving lands (SV-05). */
export function InsightPanel() {
  return <EmptyPanelState title="Insight Panel" />;
}
