MINIO_KEY=changeme-minio-user
MINIO_SECRET=changeme-minio-password

POSTGRES_USER=changeme-pg-user
POSTGRES_PASSWORD=changeme-pg-password
POSTGRES_PORT=5432

PREFECT_API_KEY=

GROQ_API_KEY=
GOOGLE_AI_STUDIO_API_KEY=

# providers retire models without notice, so both pins are overridable here.
# leave blank to take the default in app/agentic/config.py.
# check what a key can still reach:
#   curl -s https://api.groq.com/openai/v1/models -H "Authorization: Bearer $GROQ_API_KEY"
GROQ_MODEL=
GEMINI_MODEL=

GLOOMBERG_API_TOKEN=
GLOOMBERG_ORCH_BACKEND_API_TOKEN=

GLOOMBERG_DOMAIN=